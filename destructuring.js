// Destructuring

// String (brackets)
const [l, e, ...tters] = 'Cod3r'
console.log(l, e, tters)

// Array (brackets)
const [x, y] = [1, 2, 3]
console.log(x, y)

// Attributes of an object (braces)
const { age: i, name } = { name: 'John', age: 9 }
console.log(i, name)